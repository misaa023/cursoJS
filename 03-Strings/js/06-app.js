// TODO: repeat -> repite la cadena tantas veces como se indique

const producto = ' Monitor 20 Pulgadas ';

const bebidas = 'Coca Cola, Agua, Té';

console.log(producto.repeat(3));

// TODO: split -> convierte la cadena en un array
// split recibe un parametro que es la cadena por la que se va a dividir

console.log(producto.split(' '));

// separar por coma
console.log(bebidas.split(','));
