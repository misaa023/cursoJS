// TODO: arrow functions

const sumar = (a, b) => {
    return a + b;
};

const resultado = sumar(2, 2);

console.log(resultado);

// si la function tiene solo una linea de instrucciones, podemos simplificar la sintaxis

const sumar2 = (a, b) => a + b;

console.log(sumar2(2, 5));
