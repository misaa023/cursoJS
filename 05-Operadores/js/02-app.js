// TODO: COMPARA DOS VALORES

const num1 = 10;
const num2 = 20;

// compara dos valores, devuelve true o false
console.log(num1 == num2); // compara que sean iguales
console.log(num1 != num2); // compara que sean distintos

// compara dos valores, de manera estricta (tipo), devuelve true o false

console.log(num1 === num2); // compara que sean del mismo tipo
console.log(num1 !== num2); // compara que no sean del mismo tipo, pero distintos valores
