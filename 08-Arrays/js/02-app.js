// TODO: acceder a un arreglo

// ! NOTA: los indices de las matrices comienzan en 0
const autos = ['BMW', 'Mercedes', 'Volvo'];

console.table([autos]);

// acceder a un elemento con su indice
console.log(autos[0]);
console.log(autos[2]);

// acceder a un elemento que no existe
console.log(autos[10]);
console.log(autos[-1]);
