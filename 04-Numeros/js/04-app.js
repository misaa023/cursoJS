// TODO: CONVERSIONES DE NÚMEROS

const num1 = 20;
const num2 = '30';

console.log(Number.parseInt(num2)); // convierte a entero

console.log(Number.parseFloat(num2)); // convierte a flotante

console.log(Number.isInteger(num1)); // verifica si es un número entero
